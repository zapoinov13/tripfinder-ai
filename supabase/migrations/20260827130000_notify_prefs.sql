/**
 * Настройки уведомлений партнёра.
 *
 * Раньше сигналы приходили обо всём подряд: новая заявка, сообщение,
 * отзыв. У активного зала это десятки уведомлений в день, и важное
 * теряется. Теперь партнёр выбирает, о чём его беспокоить.
 *
 * Пустой объект = получать всё (поведение по умолчанию не меняется).
 */

ALTER TABLE public.profiles
  ADD COLUMN IF NOT EXISTS notify_prefs jsonb NOT NULL DEFAULT '{}'::jsonb;
